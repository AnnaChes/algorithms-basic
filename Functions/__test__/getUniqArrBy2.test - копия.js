describe('getArrObjSortedByProp.test', () => {
  it('test getArrObjSortedByProp.test', () => {

    const vasya = { name: 'Вася', age: 25 }
    const petya = { name: 'Петя', age: 30 }
    const masha = { name: 'Маша', age: 28 }
    
    const arrIn = [vasya, petya, masha]
    const prop = 'age'
    
    sortByAge(arrIn, prop); // [vasya, masha, petya] > [‘Вася’, ‘Маша’, ‘Петя’]
    
    function sortByAge(arrIn, prop) {
      
      return result
    }
  
    console.info('info', { res: getUniqArrBy(['class', 'fare'], arr) })


  })
})